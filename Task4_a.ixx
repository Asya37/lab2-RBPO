module;
#include <math.h>
export module student4:a;
namespace RBPO
{
	namespace Lab2
	{
		namespace Variant2
		{
			namespace Task4
			{
				double a(int i)
				{
					return (pow(-1, i) * (i + 1) / (pow(i, 3) + 2));

				}
			}
		}
	}
}